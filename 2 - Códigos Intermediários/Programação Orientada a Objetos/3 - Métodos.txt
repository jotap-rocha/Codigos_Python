class classe:				-> Declaração da classe

	def __init__(self, args):	-> método iniciador, onde se declara os atributos da classe
	self.args = valor
	.
	.
	.

	def metodo(self, args):		-> Método público
		contrução
		.
		.
		.

	def _metodo(self, args):	-> Método privado, só pode ser usado dentro da classe
		contrução
		.
		.
		.